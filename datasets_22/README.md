This is the folder that contains 22 UCI datasets, which were used in the uniLasso paper (Chatterjee et al., 2025). 
For the 22 datasets, rows with missing y-values were removed already.
For those UCI datasets with multiple y targets, we only kept the first target.